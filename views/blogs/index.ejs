<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Index</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Blog Posts</h1>
        <a href="/blogs/new" class="create-btn">Create New Blog Post</a>
        
        <h2>All Posts</h2>
        <div class="blog-list">
            <% blogs.forEach(blog => { %>
                <div class="blog-card">
                    <h3><a href="/blogs/<%= blog._id %>"><%= blog.title %></a></h3>
                    <p><strong>Author:</strong> <%= blog.author.username %></p>
                    <p><strong>Date:</strong> <%= blog.date ? blog.date.toLocaleDateString() : 'Not specified' %></p>
                    <form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" class="delete-btn">Delete</button>
                    </form>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
